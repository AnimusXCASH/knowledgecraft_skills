# Registry contract

Keep source ID, full hash, all paths, status, revision link, extracted path, artifact paths, history, and errors. Runtime registry state normally belongs in the consuming project, not this public skills repository.
